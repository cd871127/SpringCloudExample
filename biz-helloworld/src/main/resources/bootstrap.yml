spring:
  application:
    name: biz-helloworld
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        serviceId: service-config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8881